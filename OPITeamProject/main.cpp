#include "input.h"

int main() {
	//Function dataEntry example:
	//map <string, vector<int>> var = dataEntry();

	//Function inputFromFile example:
	//map <string, vector<int>> dataMembers = inputFromFile();

	//Function writeToFile example:
	/*vector<pair<string, vector<int>>> rangeDataMembers = {
	{ "Evchuk", { 10, 12, 0 } },
	{ "Kislov", {8, 9, 0} },
	{ "Bakumenko", {13, 14, 10} }
	};
	writeToFile(rangeDataMembers);*/
	return 0;
}